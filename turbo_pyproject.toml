[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "stagehand-turbo"
version = "0.1.0"
requires-python = ">=3.10"
description = "Rust transfer engine for Stagehand block-swap training"

[tool.maturin]
features = ["pyo3/extension-module"]
