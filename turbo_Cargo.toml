[package]
name = "stagehand-turbo"
version = "0.1.0"
edition = "2021"

[lib]
name = "stagehand_turbo"
crate-type = ["cdylib"]
path = "turbo_src/lib.rs"

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }

[features]
gpu-tests = []
